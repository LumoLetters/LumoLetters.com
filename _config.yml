# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: LumoLetters
email: info@lumoletters.com
description: >- # this means to ignore newlines until "baseurl:"
  LumoLetters provides personalized letters for seniors. Bringing joy and meaningful experiences to life.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://<your-username>.github.io/LumoLetters.com" # the base hostname & protocol for your site
twitter_username: lumoletters
github_username: <lumoletters>

# Build settings

plugins:
  - jekyll-feed

# Enable SCSS and Sass preprocessing for custom styles
sass:
  style: compressed # Compresses the final output
  load_paths:
    - "_sass"  # Allows importing partials in the _sass folder

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

# Optional: Add settings for your custom collections or paths
# collections:
#   mycollection:
#     output: true
#   anothercollection:
#     output: false
